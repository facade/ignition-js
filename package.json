{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@vue/devtools": "^5.3.3",
    "concurrently": "^5.1.0",
    "lerna": "^3.20.2"
  },
  "scripts": {
    "setup": "lerna bootstrap && lerna run setup",
    "build": "lerna run build",
    "watch": "concurrently 'vue-devtools' 'lerna run --parallel watch'"
  }
}
