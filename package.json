{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "setup": "lerna bootstrap && lerna run setup",
    "build": "lerna run build",
    "watch": "lerna run --parallel watch"
  }
}
